
(function($){
	$(function() {
		//var pathname = window.location.pathname;
		
		
		
		
		/*$(document).on('click','.item-wrap a',function(e) {	
			var container = $(this);
			var imggroup = container.data('imggroup');
			imgid = container.data('pushurl');
			var filename = imggroup+ '/' +imgid;			

			// HISTORY.PUSHSTATE
			history.pushState(null, null, '/'+filename);
			e.preventDefault();
					
		});
		
		$('.thumb-page.current-page .img-item').each(function(e){
			if($(this).hasClass('current-item')) {	
				var container = $('.item-wrap a');
				var imggroup = container.data('imggroup');
				imgid = container.data('pushurl');
				var filename1 = imggroup+ '/' +imgid;			
				
				// HISTORY.PUSHSTATE
				history.pushState(null, null, '/'+filename1);
				e.preventDefault();	
			}
		});*/	
		
		/*$('.next-btn, .prev-btn').click(function(e) {	
			var container = $('.item-wrap a');
			var imggroup = container.data('imggroup');
			imgid = container.data('pushurl');
			var filename1 = imggroup+ '/' +imgid;			
			
			// HISTORY.PUSHSTATE
			history.pushState(null, null, '/'+filename1);
			e.preventDefault();	
		});*/
		
		
		
		
	});
})(jQuery);